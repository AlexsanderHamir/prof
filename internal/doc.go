// Package internal holds shared configuration ([Config], [BenchArgs]), filesystem helpers, and
// path constants used across cli and engine packages. Keep feature orchestration in engine
// packages and compose them via [app.Services] at the CLI boundary.
package internal
