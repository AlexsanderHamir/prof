# Live TTY QA (LIVE-01)

Run from `tests/qa-ui-manual` in **Windows Terminal**:

```powershell
go run ../../cmd/prof ui
```

Capture screenshots for:

1. Hub menu (full screen)
2. Manage prof.json configuration overview
3. Compare regression policy text
4. Return to main menu prompt after collect
5. Collection filter preview before profile select

Save PNG files in this directory and mark LIVE-01 verified in [PROGRESS.md](../PROGRESS.md).
