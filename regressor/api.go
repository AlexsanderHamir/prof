package regressor

// Basic building block: comparing two tags
// Create black box test for this

// 1. Receive two tags
// 2. Get all data
// 3. Get full function name
// 4. Compare

func CheckPerformanceDifferences(tagPath1, tagPath2, benchName string) {
	// textDirPath1 := filepath.Join(tagPath1, shared.ProfileTextDir, benchName)
	// textDirPath2 := filepath.Join(tagPath2, shared.ProfileTextDir, benchName)

	//
}
